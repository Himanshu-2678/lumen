services: 
  lumen:
    build: .
    container_name: lumen-app

    ports: 
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./chroma_db:/app/chroma_db

    restart: unless-stopped